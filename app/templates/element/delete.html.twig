{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.element_delete'|trans({'%id%': element.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.element_delete'|trans({'%id%': element.id|default('')}) }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.delete'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('element_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}